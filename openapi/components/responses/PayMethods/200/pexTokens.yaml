type: array
description: Section containing saved PEX tokens
items:
  type: object
  properties:
    accountNumber:
      type: string
    payType:
      type: string
    value:
      type: string
    name:
      type: string
    brandImageUrl:
      type: string
    preferred:
      type: boolean
    status:
      type: string
      enum: [NEW, ACTIVE, EXPIRED]
