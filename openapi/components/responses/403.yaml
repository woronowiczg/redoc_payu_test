type: object
properties: 
  status:
    type: object
    properties: 
      error:
        type: string
        description: Error type.
        enum: [access_denied]
      error_description:
        type: string
        description: Description of error.
        enum: [Access is denied]
